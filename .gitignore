# 依赖目录
node_modules/
package-lock.json

# 构建输出
build/

# 操作系统文件
.DS_Store
Thumbs.db

# 编辑器和IDE文件
.cursor/
.idea/
.vscode/
*.swp
*.swo
.project
.classpath
.settings/
memo.md

# SSH config files (contain secrets)
servers.yaml
servers.yml
*.servers.yaml
*.servers.yml
ssh-config.yaml
ssh-config.yml

# SSH keys and other credentials — never commit these
*.pem
*.key
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.pub
.env
.env.*

# execute-command persistent output logs.
# These can contain remote file contents, hostnames, and command output.
.handfree-output/
# Local secrets / scratch
pass/
.omx/
